server:
  port: 8099

elasticsearch :
  clusternodes : localhost:9200
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/elastic?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
